# 고쳐 쓰는 법

이 프로그램은 **채점이 이상하면 직접 고쳐 쓰라고** 소스를 열어 뒀습니다.
포크해서 혼자 쓰셔도 되고, 고친 걸 되돌려 주셔도 좋습니다.

---

## 1. 뭐가 어디에 있나

받은 폴더는 이렇게 생겼습니다 (전부 한 폴더에 평평하게 들어 있습니다).

| 파일 | 하는 일 |
|---|---|
| `코코시험장.pyw` | 시험장 런처. 보통 이걸 더블클릭합니다 |
| `코코채점.pyw` | 채점만 하고 싶을 때 |
| `시험장.py` | 런처 본체 — 타이머, 일정, 세트 인식, 기록 |
| `grade.py` | **채점기 본체.** 채점이 이상하면 여기입니다 |
| `루틴.html` | 웹 루틴 페이지 (프로그램에서 [루틴 열기]) |
| `기대값/` | 세트별 채점 기준 JSON |
| `정정/점수정정.json` | 특정 응시의 점수를 강제로 정정 |
| `모의고사/` `드릴/` | 연습용 문제·정답 파일 |
| `version.json` | 자동 업데이트가 읽는 버전표 |

---

## 2. 채점이 틀렸을 때 — 고칠 곳 순서

**(1) 먼저 리포트에서 직접 고칠 수 있습니다**

코드를 건드리기 전에, 성적표에서 그 항목을 눌러 "내가 맞았다"로 뒤집으면
점수가 바로 되돌아옵니다. 채점기는 1차 판단이고 최종 판단은 본인입니다.
한 번만 그런 거면 이걸로 끝내는 게 제일 빠릅니다.

**(2) 그 세트에서만 계속 틀린다 → `기대값/` JSON**

세트마다 "이 문제는 이렇게 채점한다"가 JSON으로 들어 있습니다. 배점,
채점 대상 셀, 허용하는 다른 답 같은 걸 코드를 안 고치고 조정할 수 있습니다.

**(3) 모든 세트에서 같은 식으로 틀린다 → `grade.py`**

채점기 자체의 문제입니다. `grade.py` 안에서 영역별로 함수가 나뉘어 있으니
해당 부분을 찾아 고치세요. 고쳤으면 **반드시 아래 테스트를 돌리세요.**

---

## 3. 테스트 돌리기

고친 게 다른 걸 망가뜨리지 않았는지 확인하는 장치입니다.
스위트는 대상 모듈과 같은 폴더에 들어 있습니다(v3.2.0 이상) —
채점 쪽은 `채점\`, 시험장 쪽은 `시험장\` 안입니다.
**README 3단계(폴더 정리)를 마친 뒤** 그 폴더 안에서 실행하세요.

```
cd 채점
python 회귀테스트.py
```

채점기 회귀 스위트입니다. 지금까지 나온 오탐·오채점 신고가 전부
재현 테스트로 박혀 있어서, 하나라도 깨지면 예전 버그가 되살아난 겁니다.
(openpyxl 필요)

```
cd 시험장
python 일정테스트.py
python 세트인식테스트.py
python 모니터타이머테스트.py
python 재채점테스트.py
python 연동서버테스트.py
python 자동업데이트테스트.py
python 시작안정화테스트.py
python 사본변환테스트.py
python 적응형테스트.py
```

시험장 쪽 스위트입니다. 전부 tkinter 없이 도는 헤드리스 테스트라
화면 없는 환경에서도 돌아갑니다. 합쳐서 1,000건이 넘습니다.

GUI까지 실제로 띄워서 확인하려면:

```
cd 시험장
python 시험장.py --smoke
```

**새 기능을 넣었으면 테스트도 같이 넣어 주세요.** 위 파일들에 같은 방식으로
한 줄 추가하면 됩니다.

---

## 4. 되돌려 주실 때

1. 포크 → 브랜치 만들기
2. 고치고, 위 테스트 전부 통과시키기
3. Pull Request. **뭐가 어떻게 틀렸었는지**를 적어 주세요 —
   가능하면 재현되는 파일이나 상황까지.

채점 오류 제보는 이슈로 주셔도 됩니다. 이때도
**내 답 / 정답 / 채점기가 뭐라고 했는지** 세 가지가 있으면 고치기 쉽습니다.

---

## 5. 지켜 주셨으면 하는 것

- **시중 교재나 기출문제를 저장소에 올리지 마세요.** 저작권 문제가 됩니다.
  들어 있는 연습 문제는 전부 직접 만든 것이고, 그래서 공개할 수 있습니다.
  본인 문제지는 각자 자기 폴더에 넣어 쓰면 됩니다.
- **시험 시행처와 무관한 비공식 도구**라는 점이 흐려지지 않게 해 주세요.
- 개인 기록(점수·풀이 파일)은 커밋하지 마세요.

---

## 6. 개발 환경

- Windows + Excel 2021 기준으로 만들었습니다
- Python 3.10 이상 · `pip install -r requirements.txt`
- 헤드리스 테스트는 리눅스에서도 돕니다. GUI·Excel 연동은 Windows가 필요합니다
